#!/usr/bin/env node
console.log(`This script is a placeholder launcher for the skill workflow.
Use the SKILL.md instructions to generate the feature library safely.

Required policy:
- write only inside projects/<feature>;
- no macro component;
- each component in its own folder;
- NgModule + routing + NgRx;
- scoped responsive/dark mode delegations.`);
